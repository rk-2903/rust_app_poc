# Generated by Cargo
# will have compiled files and executables
/target
.DS_Store

# These are backup files generated by rustfmt
**/*.rs.bk

# ONNX model weights (~109MB) — too large for the repo; fetched by
# scripts/download_models.sh. burn-onnx's generated Rust code from these
# lives in $OUT_DIR (also not committed) and is regenerated on every build.
/models/
